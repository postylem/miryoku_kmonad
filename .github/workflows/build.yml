name: 'Build postylem'
on:
  - workflow_dispatch
jobs:
  test:
    uses: ./.github/workflows/main.yml
    with:
      mode: '["build"]'
      nav: '["invertedt"]'
      clipboard: '["mac"]'
      mapping: '["noreverseangle"]'
      kmonad_os: '["mac"]'
